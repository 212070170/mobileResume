
'use strict';

define(['angular',
    'controllers-module',

], function(angular, controllers) {

    // Controller definition
    controllers.controller("techCtrl", ["$scope", "$rootScope", function($scope, $rootScope) {

    }]);
});